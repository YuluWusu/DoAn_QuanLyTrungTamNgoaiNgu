<UserControl x:Class="DoAn_QuanLyTrungTamNgoaiNgu.Views.UC_DiemDanh"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DoAn_QuanLyTrungTamNgoaiNgu.Views"
             xmlns:vm="clr-namespace:DoAn_QuanLyTrungTamNgoaiNgu.ViewModels"
             xmlns:helpers="clr-namespace:DoAn_QuanLyTrungTamNgoaiNgu.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1280">

    <UserControl.DataContext>
        <vm:VM_DiemDanh/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <helpers:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />

        <DropShadowEffect x:Key="CardShadow" BlurRadius="16" ShadowDepth="2" Direction="270" Color="#1E293B" Opacity="0.06"/>
        <DropShadowEffect x:Key="BadgeShadow" BlurRadius="8" ShadowDepth="1" Direction="270" Color="#1E293B" Opacity="0.10"/>

        <SolidColorBrush x:Key="PrimaryBrush"      Color="#4F46E5"/>
        <SolidColorBrush x:Key="PrimaryLightBrush" Color="#EEF2FF"/>

        <SolidColorBrush x:Key="SuccessBrush"      Color="#10B981"/>
        <SolidColorBrush x:Key="SuccessLightBrush" Color="#D1FAE5"/>

        <SolidColorBrush x:Key="DangerBrush"       Color="#EF4444"/>
        <SolidColorBrush x:Key="DangerLightBrush"  Color="#FEE2E2"/>

        <SolidColorBrush x:Key="WarningBrush"      Color="#F59E0B"/>
        <SolidColorBrush x:Key="WarningLightBrush" Color="#FEF3C7"/>

        <SolidColorBrush x:Key="SurfaceBrush"      Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BackgroundBrush"   Color="#F1F5F9"/>

        <SolidColorBrush x:Key="TextPrimaryBrush"   Color="#1E293B"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#64748B"/>
        <SolidColorBrush x:Key="BorderBrush"        Color="#E2E8F0"/>

        <!-- ===== COMBOBOX STYLE ===== -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter x:Name="ContentSite"
                                                      IsHitTestVisible="False"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      Margin="10,0,0,0"
                                                      VerticalAlignment="Center"/>
                                    <Path Grid.Column="1" Data="M0,0 L4,4 L8,0" Stroke="{StaticResource TextSecondaryBrush}"
                                          StrokeThickness="1.5" Margin="0,0,10,0" VerticalAlignment="Center"
                                          StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                </Grid>
                            </Border>
                            <ToggleButton Focusable="False" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                          Background="Transparent" BorderThickness="0" Opacity="0"/>
                            <Popup x:Name="Popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True" Focusable="False">
                                <Border Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8"
                                        MinWidth="{TemplateBinding ActualWidth}" MaxHeight="200"
                                        Effect="{StaticResource CardShadow}">
                                    <ScrollViewer>
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== DATEPICKER STYLE ===== -->
        <Style x:Key="ModernDatePicker" TargetType="DatePicker">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        </Style>

        <!-- ===== BUTTON STYLE ===== -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="💾" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#4338CA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#3730A3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== SHADOW EFFECT ===== -->

        <!-- ===== DATAGRID STYLE ===== -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFBFF"/>
            <Setter Property="ColumnHeaderHeight" Value="40"/>
            <Setter Property="RowHeight" Value="46"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
        </Style>

        <Style x:Key="ModernColumnHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Height" Value="40"/>
        </Style>

        <Style x:Key="ModernDataGridRow" TargetType="DataGridRow">
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernDataGridCell" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ===== RADIOBUTTON STYLE ===== -->
        <Style x:Key="AttendancePill_Present" TargetType="RadioButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Pill" Background="{StaticResource SuccessLightBrush}"
                                CornerRadius="20" Padding="12,4" Margin="3,0">
                            <TextBlock x:Name="Label" Text="✔ Có mặt"
                                       Foreground="{StaticResource SuccessBrush}"
                                       FontSize="11" FontWeight="SemiBold"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Pill" Property="Background" Value="{StaticResource SuccessBrush}"/>
                                <Setter TargetName="Label" Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Pill" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AttendancePill_Absent" TargetType="RadioButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Pill" Background="{StaticResource DangerLightBrush}"
                                CornerRadius="20" Padding="12,4" Margin="3,0">
                            <TextBlock x:Name="Label" Text="✘ Vắng"
                                       Foreground="{StaticResource DangerBrush}"
                                       FontSize="11" FontWeight="SemiBold"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Pill" Property="Background" Value="{StaticResource DangerBrush}"/>
                                <Setter TargetName="Label" Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Pill" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AttendancePill_Late" TargetType="RadioButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Pill" Background="{StaticResource WarningLightBrush}"
                                CornerRadius="20" Padding="12,4" Margin="3,0">
                            <TextBlock x:Name="Label" Text="⏱ Trễ"
                                       Foreground="{StaticResource WarningBrush}"
                                       FontSize="11" FontWeight="SemiBold"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Pill" Property="Background" Value="{StaticResource WarningBrush}"/>
                                <Setter TargetName="Label" Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Pill" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- ===== ROOT ===== -->
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.6*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- ========== HEADER BAR ========== -->
        <Border Grid.Row="0" Grid.ColumnSpan="2"
                Background="White"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="24,14"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Icon + Title -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{StaticResource PrimaryLightBrush}" CornerRadius="10" Width="40" Height="40" Margin="0,0,12,0">
                        <TextBlock Text="📋" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="ĐIỂM DANH HỌC VIÊN"
                                   FontSize="16" FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Trung tâm Ngoại ngữ"
                                   FontSize="11"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>

                <!-- Filter controls -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="471,0,4,0" Grid.ColumnSpan="2">
                    <TextBlock Text="Lớp:" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox Style="{StaticResource ModernComboBox}"
          Width="160"
          ItemsSource="{Binding ListLop}" 
          SelectedItem="{Binding SelectedLop}"
          Margin="0,0,12,0">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TENLOP}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <TextBlock Text="Ngày:" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <DatePicker Style="{StaticResource ModernDatePicker}"
                                Width="130"
                                SelectedDate="{Binding SelectedDate}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ========== LEFT PANEL — ĐIỂM DANH ========== -->
        <Border Grid.Row="1" Grid.Column="0"
                Background="White"
                CornerRadius="14"
                Margin="16,0,8,16"
                Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Panel header -->
                <Border Grid.Row="0" CornerRadius="14,14,0,0" Padding="18,14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="📝" FontSize="15" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Danh sách học viên"
                                       FontSize="14" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                        <!-- Legend pills -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Background="{StaticResource SuccessLightBrush}" CornerRadius="20" Padding="8,3" Margin="4,0">
                                <TextBlock Text="✔ Có mặt" Foreground="{StaticResource SuccessBrush}" FontSize="10" FontWeight="SemiBold"/>
                            </Border>
                            <Border Background="{StaticResource DangerLightBrush}" CornerRadius="20" Padding="8,3" Margin="4,0">
                                <TextBlock Text="✘ Vắng" Foreground="{StaticResource DangerBrush}" FontSize="10" FontWeight="SemiBold"/>
                            </Border>
                            <Border Background="{StaticResource WarningLightBrush}" CornerRadius="20" Padding="8,3" Margin="4,0">
                                <TextBlock Text="⏱ Trễ" Foreground="{StaticResource WarningBrush}" FontSize="10" FontWeight="SemiBold"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Divider -->
                <Border Grid.Row="0" Height="1" Background="{StaticResource BorderBrush}"
                        VerticalAlignment="Bottom" HorizontalAlignment="Stretch"/>

                <!-- DataGrid -->
                <DataGrid Grid.Row="1"
                          Style="{StaticResource ModernDataGrid}"
                          ItemsSource="{Binding ListDiemDanh}"
                          AutoGenerateColumns="False"
                          ColumnHeaderStyle="{StaticResource ModernColumnHeader}"
                          RowStyle="{StaticResource ModernDataGridRow}"
                          CellStyle="{StaticResource ModernDataGridCell}"
                          Margin="0">
                    <DataGrid.Columns>
                        <!-- STT -->
                        <DataGridTextColumn Header="#" Binding="{Binding MaHV}" Width="46" IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Margin" Value="14,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Họ tên -->
                        <DataGridTextColumn Header="HỌ VÀ TÊN" Binding="{Binding HoTen}" Width="*" IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                    <Setter Property="FontWeight" Value="Medium"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Margin" Value="14,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Trạng thái -->
                        <DataGridTemplateColumn Header="TRẠNG THÁI" Width="260">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                                        <RadioButton Style="{StaticResource AttendancePill_Present}"
                             IsChecked="{Binding TRANGTHAI, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter='Co mat'}"
                             GroupName="{Binding MaHV}"/>

                                        <RadioButton Style="{StaticResource AttendancePill_Absent}"
                             IsChecked="{Binding TRANGTHAI, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter='Vang CP'}"
                             GroupName="{Binding MaHV}"/>

                                        <RadioButton Style="{StaticResource AttendancePill_Late}"
                             IsChecked="{Binding TRANGTHAI, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter='Tre'}"
                             GroupName="{Binding MaHV}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Save button -->
                <Border Grid.Row="2" Padding="18,12"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
                    <Button Style="{StaticResource PrimaryButton}"
                            Content="LƯU ĐIỂM DANH"
                            Command="{Binding SaveCommand}"
                            HorizontalAlignment="Right"
                            Width="180"
                            Padding="0"/>
                </Border>
            </Grid>
        </Border>

        <!-- ========== RIGHT PANEL — TỔNG KẾT ========== -->
        <Border Grid.Row="1" Grid.Column="1"
                Background="White"
                CornerRadius="14"
                Margin="8,0,16,16"
                Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Panel header -->
                <Border Grid.Row="0" CornerRadius="14,14,0,0" Padding="18,14">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="📊" FontSize="15" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Tổng kết vắng mặt"
                                   FontSize="14" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Divider -->
                <Border Grid.Row="0" Height="1" Background="{StaticResource BorderBrush}"
                        VerticalAlignment="Bottom" HorizontalAlignment="Stretch"/>

                <!-- Summary stat cards -->
                <Grid Grid.Row="1" Margin="14,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Tổng vắng -->
                    <Border Grid.Column="0" Background="{StaticResource DangerLightBrush}"
                            CornerRadius="10" Padding="12,10" Margin="0,0,6,0">
                        <StackPanel>
                            <TextBlock Text="TỔNG VẮNG" FontSize="10" FontWeight="SemiBold"
                                       Foreground="{StaticResource DangerBrush}" Opacity="0.75"/>
                            <TextBlock Text="{Binding TotalAbsentCount, FallbackValue='—'}"
                                       FontSize="22" FontWeight="Bold"
                                       Foreground="{StaticResource DangerBrush}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Tổng trễ -->
                    <Border Grid.Column="1" Background="{StaticResource WarningLightBrush}"
                            CornerRadius="10" Padding="12,10" Margin="6,0,0,0">
                        <StackPanel>
                            <TextBlock Text="TỔNG TRỄ" FontSize="10" FontWeight="SemiBold"
                                       Foreground="{StaticResource WarningBrush}" Opacity="0.75"/>
                            <TextBlock Text="{Binding TotalLateCount, FallbackValue='—'}"
                                       FontSize="22" FontWeight="Bold"
                                       Foreground="{StaticResource WarningBrush}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Summary DataGrid -->
                <DataGrid Grid.Row="2"
                          Style="{StaticResource ModernDataGrid}"
                          ItemsSource="{Binding AttendanceSummaryList}"
                          ColumnHeaderStyle="{StaticResource ModernColumnHeader}"
                          RowStyle="{StaticResource ModernDataGridRow}"
                          CellStyle="{StaticResource ModernDataGridCell}"
                          IsReadOnly="True"
                          Margin="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="HỌC VIÊN" Binding="{Binding StudentName}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Margin" Value="14,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="VẮNG" Width="64">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource DangerLightBrush}"
                                            CornerRadius="6" Padding="6,3"
                                            HorizontalAlignment="Center" Margin="8,0">
                                        <TextBlock Text="{Binding AbsentCount}"
                                                   Foreground="{StaticResource DangerBrush}"
                                                   FontWeight="Bold" FontSize="12"
                                                   HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="TRỄ" Width="56">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource WarningLightBrush}"
                                            CornerRadius="6" Padding="6,3"
                                            HorizontalAlignment="Center" Margin="8,0">
                                        <TextBlock Text="{Binding LateCount}"
                                                   Foreground="{StaticResource WarningBrush}"
                                                   FontWeight="Bold" FontSize="12"
                                                   HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
